# URL Shortener

Short codes use a 62^7 (~3.5 trillion) space; the database enforces uniqueness and conflicts are retried.

TODO: raise the 512-character URL limit when product requirements warrant it.
